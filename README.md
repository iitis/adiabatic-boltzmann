Implementation of [Quantum neural networks to simulate many-body quantum systems](https://arxiv.org/pdf/1805.05462)

Install:
- `pip install -r requirements.txt -r req_torch.txt`

Run:
- `python src/main.py` to train the model and plot the results.
